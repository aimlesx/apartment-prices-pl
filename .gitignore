# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Środowiska / uv (uv.lock JEST commitowany)
.venv/
venv/

# Kedro — dane (trzymamy tylko strukturę katalogów przez .gitkeep)
training/data/**
!training/data/**/
!training/data/**/.gitkeep
# Kedro — lokalne sekrety/nadpisania
training/conf/local/**
!training/conf/local/.gitkeep
info.log
logs/
.viz/

# MLflow
mlruns/
mlartifacts/
mlflow.db

# Wyeksportowany model (mount dla Dockera — artefakt builda, nie do repo)
deploy/model/

# Artefakty modeli
*.pkl
*.joblib

# Sekrety / env
.env
*.env

# OS / IDE
.DS_Store
.idea/
.vscode/

# Frontend (web/) — zależności i artefakty builda (poza repo; package-lock.json JEST commitowany)
web/node_modules/
web/dist/

# Dokumenty wewnętrzne (planowanie / notatki — poza repozytorium)
internals/
